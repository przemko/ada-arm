package Driver is

   task Controller_1 with
     Storage_Size => (4 * 1024);

   task Controller_2 with
     Storage_Size => (4 * 1024);

   task Controller_3 with
     Storage_Size => (4 * 1024);

end Driver;
